apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitly-ingest.fullname" . }}
  labels:
    {{- include "bitly-ingest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bitly-ingest.selectorLabels" . | nindent 4 }}
