apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bitly-ingest.fullname" . }}-test-connection"
  labels:
    {{- include "bitly-ingest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bitly-ingest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
